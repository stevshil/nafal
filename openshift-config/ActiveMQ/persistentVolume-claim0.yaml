apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: activemq-claim0
  name: activemq-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
status: {}
